# 第三者の権利について

このリポジトリのコードは [MIT ライセンス](LICENSE)。
ただし**動かすと外部のものを引いてくる**ため、それぞれの条件が別に掛かる。
配布・公開する場合はここを確認すること。

## 実行時に取得するモデル（リポジトリには含まれない）

`ollama` が起動時に取得する。**重みはこのリポジトリに同梱していない**ので、
再配布しているのはあくまで「取得する指示」だけ。

| モデル | ライセンス | 注意 |
|---|---|---|
| `gemma3:1b` | **Gemma Terms of Use**（Google） | MIT ではない。利用と再配布に条件がある。派生物にも同じ条件を課す必要がある |
| `qwen2.5:1.5b` | **Apache License 2.0** | 帰属表示を保つこと |

確認は `docker compose exec ollama ollama show <モデル名>`。
`CHAT_MODELS` を差し替える場合は、**そのモデルの条件を必ず自分で確認すること**。

## 実行時に呼ぶ外部サービス

| 先 | 条件 | このリポジトリでの対応 |
|---|---|---|
| **日本語版 Wikipedia**（調べ物） | 本文は **CC BY-SA**。API 利用は User-Agent の明示が求められる | 回答に項目名と**出典リンク**を必ず添える（`apps/chat-ai/wikipedia.py`）。UA も名乗っている |
| **気象庁**（天気） | 正式な公開 API ではない。政府標準利用規約に準拠して利用 | 予報文を加工せずそのまま渡す。TTL キャッシュで取得回数を抑える（`apps/chat-ai/weather.py`） |
| **Open-Meteo**（海外の天気） | 非商用は無料・キー不要 | 商用で使う場合は先方の条件を確認すること |

⚠ Wikipedia の CC BY-SA は「出典と同じ条件での再配布」を求める。
本文をそのまま画面に出すのは引用の範囲だが、**保存して再配布する用途に広げる場合は要検討**。

## 同梱している画像

`apps/chat-gateway/ui.html` に埋め込んだ「こてつ」のアイコンは、
`ec-api` の商品画像（`images/products/kotetsu.jpg`）から切り出したもの。
同じ作者の著作物なので MIT の範囲に含まれる。
`apps/chat-ai/content/avatar.jpg` も同じ出どころ。

## 同梱している人格（黒猫武将こてつ）

`apps/chat-ai/content/` 一式は**見本**として同梱している。
**この人格は配布元の沢蛍由来のもの**なので、自分の設置で使うなら差し替えてほしい。

これはライセンス上の制約ではなく（MIT なので改変も再配布も自由）、
**名乗りの筋の話**。枠組みは人格を差し替える前提で作ってあり、
差し替えても意図分類・順番待ち・レート制限はそのまま動く。
まっさらから始めるなら `examples/content-minimal/` を写して使う。

## 依存ライブラリ

いずれも広く使われている寛容なライセンス（詳細は各パッケージを参照）:

- Go: 標準ライブラリのみ（外部依存なし）
- Python: `fastapi` / `uvicorn` / `httpx`（すべて MIT または BSD 系）
- 実行基盤: Caddy（Apache-2.0）、PostgreSQL（PostgreSQL License）、Docker イメージ各種
