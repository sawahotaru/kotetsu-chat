# 変更の記録 — チャット（chat-gateway / chat-ai）

配る単位は `apps/chat-gateway` ＋ `apps/chat-ai` ＋ `examples/` ＋
`LICENSE` ＋ `THIRD-PARTY.md`。版はこの二つで**揃えて**付ける
（別々に上げると、どの組み合わせが動いているのか誰にも分からなくなる）。

版の正は次の三箇所。**ずらさないこと。**

| 場所 | 何のため |
|---|---|
| `apps/chat-ai/settings.py` の `VERSION` | 後段。`/healthz` と起動ログに出る |
| `apps/chat-gateway/main.go` の `version` | 前段。同上 |
| このファイルの見出し | 人が読む |

版の付け方は [Semantic Versioning](https://semver.org/lang/ja/)。
1.0.0 より前なので、**細かい変更でも中身が入れ替わることがある**。

---

## 0.1.3 — 2026-08-30

**利用者に決めさせてはいけない物が、前段を素通りしていた。塞いだ。**

いずれも「LLM を切ってあるので、今は動いていない」だけで、
灯した瞬間に効いてしまう類のもの。LINE の口を足すと ollama は常時動くようになるため、
**その前に閉じる**。

### 直したもの

- **`system` / `temperature` が匿名の利用者から素通しだった**（前段）。
  後段は `system` が空でなければ既定を捨てて採用する（`req.system.strip() or DEFAULT_SYSTEM`）。
  つまり外から 2000 文字ぶんの指示を差し込め、**公開している案内役に何でも言わせられた**。
  画面は LLM 無効時にこの欄を隠していたが、**隠れていたのは画面だけで API は受け付けていた**。
  守りを画面に置いてはならぬ。
  → `CHAT_ALLOW_CLIENT_PARAMS`（既定 **0**）を足し、偽なら前段が空に落とす。
  **弾かずに黙って捨てる**——画面は欄が隠れていても temperature を常に送っており
  （スライダの既定 0.7）、弾く作りにすると公開版の会話が丸ごと 400 になる。
- **利用者の文面をログに残していた**（後段）。
  `ルートで拾えず（LLM無効）: %s` に `last_user[:60]` を添えていた。
  この枝は LLM が無効なとき——**公開している Web の口では毎回**通る。
  しかも通るのは「規則で拾えなかった問い」＝最も踏み込んだ、その人固有の質問だけ。
  一方こちらは聞かれれば「会話は残さぬ」と答える。**答えと振る舞いが食い違っていた。**
  → 文面を落とし、拾えたか否かだけ残す。閾値の調整にはそれで足りる。

### 変えたもの

- `/healthz` に `client_params` を足した。画面はこれを見て、
  **前段が捨てる欄（temperature・システムプロンプト）を最初から出さない**。
  「動かしたのに効かない」を作らないため。守りは前段のままで、画面は見た目を合わせるだけ
- 起動ログに `利用者設定=` を足した

### 配る側への注意

`CHAT_ALLOW_CLIENT_PARAMS=1` は**手元で模型の振る舞いを試すとき専用**。
公開する設置で開けてはならぬ。

---

## 0.1.2 — 2026-08-27

**「なぜ LLM を使っていないのか」に、案内役自身が答えられるようにした。**

公開している lab では LLM を切ってある。**機能が無いのではなく、公開の窓口だけ切ってある。**
理由は二つで、①4コア1台に EC も予約も同居しており推論中は隣が遅くなる（しかも匿名で叩き放題）、
②1B 級の模型はこの場に実在する品を「取り扱っておりません」と平然と申す。
**その判断そのものが見どころ**なのに、聞かれても答えられなかった。
口ごとに切り替えられる作りなので、素性の知れた口（LINE）で灯す見込みも併せて述べる。

### 加えたもの

- **`〔LLM:名前〕`（名前つきの差し替え）** — `persona.yaml` の `llm_note` に
  入れ子で書いた長い文を、回答文から呼べるようにした。
  一文の差し替え（`〔LLM〕`）では足りない、**答えが丸ごと入れ替わる**問いのため。
  有りの設置と無しの設置で書くことがまるきり違うので、
  どちらか一方に固定すると**もう一方で嘘になる**
- **意図 `llm_off`** — 「なぜ LLM を使っていないの」「LLM を切っている理由は」
  「ollama は動いていないの」等。`demo_chat`（作りの説明）とは別に扱う
- 見つけられるように、頭出しの案内と「何ができる？」の一覧にも一行足した
- **人格の出どころを明記した** — 「この人格（黒猫武将こてつ）は配布元の沢蛍由来のものです。
  自分の設置で使うなら差し替えてください」を内容フォルダの手引き・`persona.yaml` の冒頭・
  `THIRD-PARTY.md`・単体 compose の手引きに。ライセンス上の制約ではなく**名乗りの筋の話**
  （MIT なので改変も再配布も自由）
- 試験 — 名前つきの差し替え（`test_persona.py`・4件）、
  `llm_off` と `demo_chat` の境目（6件）、
  **どちらの設定でも目印が残らないこと**（綴りを間違えると画面に出るため）

---

## 0.1.1 — 2026-08-26

**配った先で本当に動くか**を実際に確かめたら、手引きが3つ嘘をついていた。その修正。

### 直したもの

- **`examples/standalone/` をコピーしても動かなかった。**
  `build:` が `../../apps/...` を指しており、フォルダの外が要る。
  持ち出し用のフォルダを作る `bundle.sh` を足し、
  compose のビルド元を `${CHAT_SRC:-../..}` にした。
  同じ compose が repo の中でも持ち出した先でも通る（写しを持たない）
- **手引きどおりに連携（`my_shop.py`）を置いても読み込まれなかった。**
  置き場所が書かれておらず、イメージの中にしか置けなかった。
  **内容ファイルの隣**（`CHAT_CONTENT_DIR` の中）に置けるようにした。
  連携は「この設置の中身」の一部なので、人格や回答と同じ所に置くのが筋。
  ⚠ 枠組みと同じ名前を置かれても枠組みが勝つ（探し場所は末尾に足す）
- **内容を差し替えると `pytest` が130件以上の失敗を並べた。**
  意図分類の一覧は同梱の内容に向けて書いてあるので当然だが、
  理由が分からず「壊した」と読める。半分より多くの意図が見当たらなければ
  **理由を告げて丸ごと飛ばす**ようにした（枠組み自体の試験は走り続ける）

### 追加

- `examples/standalone/bundle.sh` … 持ち出せる一つのフォルダを作る
- 試験2件 … 内容ファイルの隣の連携が読めること／枠組みが名前の取り合いに勝つこと

---

## 0.1.0 — 2026-08-26

配る形に整えた最初の版。

### 追加

- **チャット単体の compose**（`examples/standalone/`）。
  このフォルダを持っていけば `docker compose up -d` で動く。
  Caddy・DB・他のアプリは要らない
- **導入手順**（`examples/standalone/README.md`）。
  何ができるものか・起動・差し替え・設定一覧・**公開するときの注意**
- **設置ごとの追加処理を任意読み込みに**（`CHAT_SITE_HANDLERS` / `plugins.py`）。
  この lab に固有の `labinfo.py` は**消しても起動する**。
  消したときは、それを参照する意図（品探し・予約の空き）が起動時に自動で外れる
- **画面の文言も内容ファイルへ**（`persona.yaml` の `ui.labels`）。
  「送信」「会話をクリア」等 21 の文言を差し替えられる
- **地域依存を切り離す口**。`WEATHER_SOURCE=open-meteo` で気象庁を使わず、
  `content/places.yaml` で引ける地名を足せる。`WIKIPEDIA_LANG` で言語版を選べる。
  `CHAT_USER_AGENT` で外部への名乗りを自分のものにできる
- **テストを pytest へ**。意図分類の回帰 140 ケースに加え、
  語の切り出し・連携の読み込みの単体試験。GitHub Actions で走る
- `/healthz` と起動ログに版を出すようにした

### 直したもの

- 画面の文言が、`/api/persona` と `/api/models` のどちらが先に返るかで
  案内役の言葉／中立の既定に割れていた（器を起こした直後だけ文面が違った）
- `CHAT_CONTENT_DIR` に空文字を渡すと `.` を見に行き、起動が止まっていた

### 分かった上で残していること

- **多言語化はしていない。** `ui.labels` は文言を差し替えられるだけで、
  閲覧者ごとに切り替える仕掛けは無い。天気の言い表し（「くもり」「明後日」）や
  調べ物の前後の言い回しは `weather.py` / `wikipedia.py` の日本語のまま
- **意図分類は文字の重なり（N-gram）**。形態素解析も学習済みの模型も使わない。
  軽くて表記ゆれに強い代わりに、語が長いほど薄まる
- **LLM は既定で無効**。負荷ではなく中身の理由
  （`examples/standalone/README.md` の「公開するときの注意」）

---

## それ以前

非公開の統合リポジトリの中で作っていた時期。記録はそちらにある。
